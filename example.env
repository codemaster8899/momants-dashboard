# Upstream Momants (or compatible) API — server-side only
# Used by Next.js rewrites (/backend/*) and /api auth routes
BASE_API_URL=https://your-api.example.com

# Same API origin exposed to the browser (REST + WebSocket)
NEXT_PUBLIC_BASE_API_URL=https://your-api.example.com
